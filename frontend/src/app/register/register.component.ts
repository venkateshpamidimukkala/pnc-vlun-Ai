import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { AuthService, Role } from '../core/auth.service';

  @Component({ standalone: true, imports: [FormsModule, RouterLink], template: `<main class="login-page"><section class="login-brand"><span class="brand-mark">PNC</span><p class="eyebrow">Access management</p><h1>Create your secure workspace account.</h1><p>Choose a demo role to test page-level authorization.</p></section><section class="login-card"><div class="card-logo">Ojasin AI</div><h2>Register</h2><form (ngSubmit)="submit()"><label>Full name<input class="field full" name="name" [(ngModel)]="name" required></label><label>Email<input class="field full" type="email" name="email" [(ngModel)]="email" required></label><label>Password<input class="field full" type="password" name="password" [(ngModel)]="password" minlength="8" required></label><label>Role<select class="field full" name="role" [(ngModel)]="role"><option value="SECURITY_ANALYST">Security Analyst</option><option value="SECURITY_REVIEWER">Security Reviewer</option></select></label>@if(error){<p class="error">{{error}}</p>}<button class="sign-in" [disabled]="loading">{{loading ? 'Creating account…' : 'Create account'}}</button></form><p class="auth-link">Already registered? <a routerLink="/login">Sign in</a></p></section></main>`, styles: [`.full{display:block;width:100%;margin-top:.4rem}.login-card label{display:block;margin-top:1rem;font-size:.8rem;font-weight:600}.error{color:var(--danger);font-size:.8rem}.auth-link{text-align:center;font-size:.8rem}.auth-link a{color:var(--orange-dark);font-weight:700}`] })
export class RegisterComponent { private readonly auth = inject(AuthService); private readonly router = inject(Router); name = ''; email = ''; password = ''; role: Role = 'SECURITY_ANALYST'; loading = false; error = ''; submit(): void { this.loading = true; this.auth.register(this.name, this.email, this.password, this.role).subscribe({ next: () => this.router.navigateByUrl('/dashboard'), error: (e: { error?: { detail?: string } }) => { this.error = e.error?.detail || 'Unable to register.'; this.loading = false; } }); } }